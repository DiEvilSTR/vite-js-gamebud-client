import { useContext } from 'react';
import { AuthCtx, Page } from '/src/components';

export function GameBudPageIndex() {
  // const auth = useContext(AuthCtx);
  // const query = useQuery('todos', getTodos);

  return (
    <Page>
      <h1>GameBudList</h1>
    </Page>
  );
}
